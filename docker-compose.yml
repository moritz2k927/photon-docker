version: '2'
services:
  photon:
    build:
      context: .
      dockerfile: Dockerfile
    image: moritz2k927/photon-docker
    volumes:
      - ./photon_data:/photon/photon_data
    ports:
      - 2322:2322

volumes:
  photon_data:
    driver: local
